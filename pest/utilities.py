def compact_protein(protein):
    """Takes a list of amino acids and returns them as a string."""
    return ''.join(protein)


def print_protein(protein):
    """Takes a list of amino acids and prints them as a string."""
    print(compact_protein(protein))
